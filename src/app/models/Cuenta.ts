export interface CuentaCreacionDTO{
    nombre: string;
    tipoCuentaId: number;
    balance: number;
    descripcion: string;
}

export interface CuentaDTO{
    id: number;
    nombre: string;
    tipoCuentaId: number;
    balance: number;
    descripcion: string;
}